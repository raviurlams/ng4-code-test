export interface companyView {
    CompanyName ? : string;
    Street1 ? : string;
    Street2: string;
    City ? : string;
    State ? : string;
    Zip ? : string;
    Country ? : string;
    Telephone ? : string;
    Fax ? : string;
    LogoFile: string;
    UseLogo: boolean;
    EmailCompany ? : string;
    CompanyContact ? : string;
}
